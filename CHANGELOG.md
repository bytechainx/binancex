# 变更记录

## [0.2.0] - 2026-09-25

### 变更

- **破坏性变更**：`Quantity::new` 改为接收已校验的 `Decimal` 并直接返回 `Quantity`；调用方需先用 `Decimal::new(text)?` 解析。
- **破坏性变更**：`DataSeriesId::new` 改为接收 `DataSeriesEntity` 与语义维度；`instrument()` 访问器由 `entity()` 替代。
- **破坏性变更**：Kline 响应从元组别名改为名义类型；调用方需按各类型公开字段构造和读取。

### 新增

- 增加四族离线值类型、响应解析入口、身份冲突校验及严格深度快照解析。
- 增加 Binance API library 的类型层、离线解析与 fail-closed 授权判定文档。

### 说明

- 本 crate 仍不执行联网请求或生产接入；生产决定保持 `NO-GO`。

## [0.1.1] - 2026-09-24

### 修正

- 纳入元仓库评分与治理名册

## [0.1.0]

- 补齐 CI 三件套与 package 元数据门禁、离线解析微基准，并拆分 USDM 类型实现文件；Kline 响应改为独立名义类型，白名单快照改为四族严格解析。
- 建立四族响应值对象及离线解析入口，保留源字段命名、单双对象形态、数组和定长元组。
- 提供身份、时间、十进制、带单位数量及白名单快照值对象；DataSeriesId 支持全局主体，Quantity 接收已校验 Decimal 并保留原始数值符号。
- 提供离线授权证据解析与 fail-closed 判定，日期严格采用 `YYYY-MM-DD`；空证据、无有效证据或范围冲突均拒绝。
- 补充 API、合成夹具边界、依赖 MSRV 推导与开发门禁文档；声明 Rust 1.77。
- 记录响应合同开放点与当前语义验证限制；生产决定保持 `NO-GO`。
- 在四族 `ExchangeInfo` 列表项要求本地标的键且拒绝列表内重复项；在 Spot、USDM、COINM 聚合成交及 Spot、USDM 成交入口要求本地记录身份，并拒绝单次响应批中的重复 ID。上述策略均不宣称源方或跨请求全局唯一。
- `DataSeriesId` 分开承载原生离散变体与语义维度，避免周期相同的不同系列身份相撞。
